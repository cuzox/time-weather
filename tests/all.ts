import { expect } from 'chai'
import 'mocha'


describe('App', ()=>{
  it('#run() should return correct time', () => {
    expect('test').to.equal('test')
  })
})

describe('Utils', ()=>{
  it('#getTimeFromUTCOffset() should return correct time', () => {
    expect('test').to.equal('test')
  })

  it('#parseLocations() should return properly parsed locations', () => {
    expect('test').to.equal('test')
  })
})
